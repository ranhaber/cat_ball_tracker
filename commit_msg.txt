v1.2.4: Threshold slider, motion fixes, settings logging

Features:
- Add detection threshold slider (10%-90%) in Settings
- Lower default threshold to 30% for better small object detection
- Display version number in footer

Bug Fixes:
- Fix motion detector crash on resolution change
- Motion regions now only display inside defined perimeter

Improvements:
- Log all setting changes to journal with [SETTING] prefix
